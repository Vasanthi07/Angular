import { FiltroFetchaPipe } from './filtro-fetcha.pipe';

describe('FiltroFetchaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroFetchaPipe();
    expect(pipe).toBeTruthy();
  });
});
